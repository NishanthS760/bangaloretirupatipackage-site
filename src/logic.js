// src/logic.js - Business logic, validation and mock API (1-2 lines)\nexport const CONFIG = { MIN_BOOKING_DAYS: 2, NEXT_TRIP_DAYS: 3, PHONE_REGEX: /^[6-9]\\d{9}$/, MAX_NAME_LENGTH: 100, FORM_DEBOUNCE: 300 };\nexport const packagePrices = { weekday: 2500, weekend: 2800 };\nexport function addBusinessDays(date, days) { const d = new Date(date.getTime()); let added = 0; while (added < days) { d.setDate(d.getDate() + 1); added++; } return d; }\nexport function createDateString(d) { const y = d.getFullYear(); const m = String(d.getMonth()+1).padStart(2,'0'); const day = String(d.getDate()).padStart(2,'0'); return `${y}-${m}-${day}`; }\nexport function formatDate(d) { return d.toLocaleDateString(undefined, { year: 'numeric', month: 'short', day: 'numeric' }); }\nexport function validatePhone(value) { return CONFIG.PHONE_REGEX.test(String(value)); }\nexport function formatCurrency(value) { try{ return new Intl.NumberFormat('en-IN',{ style: 'currency', currency: 'INR', maximumFractionDigits:0 }).format(value); }catch(e){ return '₹'+value; } }\nexport function mockApiCall(url, opts = {}) { return new Promise((resolve)=>{ const seats = Math.max(0, Math.floor(Math.random()*30)); setTimeout(()=>resolve({ data:{ seats } }), 300 + Math.random()*700); }); }\nexport function debounce(fn, wait = CONFIG.FORM_DEBOUNCE) { let t; return function(...args){ clearTimeout(t); t = setTimeout(()=>fn.apply(this,args), wait); }; }\n
